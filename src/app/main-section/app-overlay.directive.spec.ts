import { AppOverlayDirective } from './app-overlay.directive';

describe('AppOverlayDirective', () => {
  it('should create an instance', () => {
    const directive = new AppOverlayDirective();
    expect(directive).toBeTruthy();
  });
});
